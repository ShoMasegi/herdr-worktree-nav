# 使い方

[English](../en/usage.md)

ピッカーは 2 つ、それぞれ対応するアクションで開き、`Tab` で切り替えます。どちらも popup として開きます。生きているセッションの上に浮く中央寄せの枠で、枠は herdr があなたの accent 色で描き、タイトルは `herdr-worktree-nav` です。大きさは session navigator と同じ比率です。

枠の内側の描画は navigator と同じ方式です。検索行・詳細行・キーヒントの構成、tree グリフ、状態グリフが揃っています。navigator が定めているキーは、ここでも同じ意味を持ちます。

ピッカーが開いている間、herdr はすべてのキーをピッカーに送るため、herdr 自身のキーバインドは閉じるまで届きません。`Esc`・`q`・`Ctrl-C` のいずれでも閉じられます。

**どこから開いたか**が重要です。キーを押した時点のリポジトリと pane を引き継ぐので、「ここに split」の「ここ」が決まり、ブランチ一覧がどのリポジトリから始まるかも決まります。

## Panes

| キー | 動作 |
| --- | --- |
| `↑` `↓`, `k` `j`, `Ctrl-P` `Ctrl-N` | 移動 |
| `←` `→`, `h` `l` | 前／次のリポジトリの先頭へ |
| pane 行で `Enter` | そこへ移動 |
| 何も動いていない checkout 行で `Enter` | その checkout を開く |
| `n` | カーソル位置の checkout に pane を追加 |
| `Shift-D` | カーソル位置の checkout を削除する（確認あり） |
| `Tab` | Branches へ（カーソル位置のリポジトリから始まる） |
| `/` | 検索 |
| `b` `w` `i` `d` | blocked / working / idle / done に絞り込み |
| `a` | 状態の絞り込みを解除 |
| `r` | 再読み込み |
| `q`, `Esc`, `Ctrl-C` | 閉じる |

`←` `→`（`↑`/`↓` に `k`/`j` があるのと同じく `h` `l` でも同じ）は、リポジトリの中で最初に行き先となる行——最初の pane、無ければ最初の「何も動いていない checkout」——に着きます。**1 押しは必ず 1 リポジトリ**で、そのリポジトリの途中にいても同じです。`←` は今いるリポジトリの先頭に戻るのではなく、そこから出ます。端では巻き戻り、リポジトリに属さない pane 群も 1 セクションとして数えます。

矢印は検索ボックスに入力中でも効きます。英字のほうは効きません。そこでは打っている文字そのものだからです。

リポジトリに属さない pane は常に一覧の末尾に独立したセクションとして出ます。それも pane であることに変わりはなく、一部を隠すピッカーは「何が隠れているのか」を気にさせるだけだからです。

カーソルは**行き先のある行にだけ止まります**。pane と、何も動いていない checkout です。リポジトリの見出し行と、既に pane を持つ checkout 行は飛ばします。その直下に並んでいる pane 自体が答えであり、見出しに一度止まるのは移動を長くするだけだからです。飛ばされる行も画面には残ります。矢印キーが通り過ぎるだけです。

### checkout を削除する

`no pane` の行で `Shift-D` を押すと、その worktree を削除するか訊きます。問いは検索フィールドの 1 行ではなく、一覧の上に重なる枠として出ます。もう一度やり直せば元に戻る、が成り立たない唯一の操作だからです。答えられるのは `y` だけで、他のキーはすべて「いいえ」です。

```
   └── · fix/crash  no pane    ~/.herdr/worktrees/app/fix-crash

        ┌──────────────────────────────────────┐
        │ Delete this checkout?                │
        │                                      │
        │   fix/crash                          │
        │   ~/.herdr/worktrees/app/fix-crash   │
        │                                      │
        │   y delete     any other key cancels │
        └──────────────────────────────────────┘
```

パスを出しているのは、消えるのがブランチではなく**そのパスの checkout** だからです。長いパスは中央を省略します（一覧の下の詳細行には全体が残ります）。枠が入らない高さのペインでは、まず空行が、次に詳細が落ちます。キーヒントはどちらの場合も同じことを言います。

実行するのは `git worktree remove <path>` だけです。**ブランチは残します**。checkout は作り直せますが、push していないブランチは作り直せないからです。`--force` は付けないので、コミットしていない変更や未追跡ファイルがあると git が拒否します。この拒否は障害ではなく機能です。

2 つは問いを出す前に断ります。pane と、pane が動いている checkout——そこに安全に消せるものはありません。それと、リポジトリ自身の checkout——git は main working tree を削除できず、そもそも worktree ではありません。

`y` はすぐ戻ってきます。削除は別のところで走ります——git は working tree 全体を歩いてからでないと消せず、それなりの大きさのリポジトリでは数秒かかります——その間、行のほうがそう言います。

```
   └── · fix/crash  deleting ⠻    ~/.herdr/worktrees/app/fix-crash
```

以降カーソルはその行を飛ばします。もうその行に対してできることはなく、次に片付けるのはたいてい 1 つ下の行だからです。続けて別の checkout を消しても、`Tab` で Branches へ移っても、ピッカーを閉じても構いません。どれも削除を待ちません。閉じても何も失われません。

どちらに転んでも、herdr が知らせます。

| | |
| --- | --- |
| 削除できた | `removed fix/crash`、下にパス、音は鳴らさない |
| 断られた | `could not remove fix/crash`、下に git の言葉そのもの、音を鳴らす |

**この通知が報告そのもの**です。ピッカーが閉じた後にも届く唯一の経路だからです。通知を切っている場合は届きませんが、失うものはありません。断られた削除は checkout をそのまま残すので、次に開けばその行があり、もう一度 `Shift-D` を押せば git の理由がまた読めます。ピッカーが開いたままなら、断られた理由はプロンプト行にも出ます。

`b`/`w`/`i`/`d` を押すと、検索ボックスが状態チップに変わります。同じキーをもう一度押すと解除されるので、絞り込みが一方通行になることはありません。

検索中は、英字はコマンドではなく文字として入力されます。`Enter` は絞り込みを残したまま上記のキー操作に戻り、`Esc` は絞り込みを破棄し、`Ctrl-U` は検索モードのまま中身だけ空にします。

### 行の読み方

```
 ◆ ● ShoMasegi/herdr-worktree-nav (2)
   └── ● main  ↑2↓1                    ~/Workspace/herdr-worktree-nav
 ◆    ├── ● claude                     w7:p2
      └── · shell                      w7:p3
   └── · fix/crash  ✱  gone  no pane   ~/.herdr/worktrees/herdr-worktree-nav/fix-crash
                                    ↑ four columns past the longest label
```

左から順に、gutter、tree、状態グリフ、ラベル、meta 列です。

- **gutter** には、いまフォーカスされている pane と、それを含むリポジトリに `◆` が付きます。
- **tree** はリポジトリ配下を `├──`/`└──` の連結グリフでつなぎます。階層が深い worktree でも、どこに属しているかが読み取れます。リポジトリ自身にはグリフが付きません。展開するものが無い見出しだからです。
- **ラベル** はリポジトリが `owner/repo (n)`（`n` は開いている pane 数）、worktree はブランチ名、pane はエージェント名（無ければ `shell`）です。
- **meta 列** は「それがどこにあるか」を示します。worktree は checkout パス、pane は pane ID です。リポジトリ行には何も出しません。直下の主 checkout が同じパスを持つためです。ホーム配下のパスは `~` に短縮します。
- 列の開始位置は右端ではなく、**中身のある行のうち最長のラベルから 4 桁後ろ**です。幅の広いペインでもパスが行の隣に留まり、画面の反対側まで目線を運ばずに済みます。そこから行末までを使うので、パスはたいてい省略なしで表示されます。それでも入らない場合は端ではなく**中央**を省略します。先頭はどのツリーの中か、末尾はどの checkout かを示すためです。
- 何も動いていない checkout は、meta 列がパスで埋まっているため、名前の脇に `no pane` と付きます。

### checkout が今どういう状態か

ブランチ名と `no pane` の間に、その checkout 自身の状態が出ます。ほとんどの行には何も付きません。列にせず名前の脇に置いているのはそのためです。ほとんどの行で空になる列は、名前とパスの間に恒久的な隙間を作るだけだからです。

| | |
| --- | --- |
| `✱` | 作業ツリーに未コミットの変更か未追跡ファイルがある |
| `↑2↓1` | upstream にない commit が 2 つ、こちらにない commit が 1 つ |
| `gone` | 追跡していたブランチがリモートにもう無い |

`✱` は `Shift-D` が通らない理由そのものです。git は誰もコミットしていない作業を抱えた checkout の削除を拒みます。これはその拒否を、キーを押す前に見せているだけです。`gone` は「そのブランチが追跡している ref を git が見つけられない」という意味です。たいていは pull request がマージされて GitHub が head を消し、Branches ビューの `f` による fetch が prune した結果ですが、一度も fetch していない upstream も同じに見えます。git にとって同じことだからです。

ahead / behind と `gone` は、ピッカーが既に実行している `git for-each-ref` から出てくるので、最初のフレームから揃っています。作業ツリーが汚れているかは違います。git はツリー全体を歩かないと分からず、しかも checkout ごとに 1 回必要です。そのためこれだけはバックグラウンドで訊き、答えが届いた行から埋めていきます。届くまでその行は何も言いません——推測するくらいなら黙るほうが良いからです——代わりにプロンプト行がスピナーを回し、「まだ埋まっている途中」が「何も見つからなかった」に見えないようにしています。

`✱` の場所は、実際に付くかどうかに関わらず最初のフレームから確保してあります。答えが届いても隣のパスが動かないようにするためです。3 桁の余白は、読んでいる最中に一覧がずれないことの対価です。

git がそもそも答えを返さないとき——`safe.directory` による拒否や、herdr がプラグインを起動した `PATH` に `git` が無い場合——は、スピナーがあった場所にそう出ます。

```
 / search panes  2 working trees unreadable                                          5 panes
```

この種の拒否は全 checkout に対して同時に起きるので、これが無いと「印の付いていない一覧」が「どこにも未コミットの作業は無い」という断定に見えてしまいます。**読めなかった作業ツリーは、`clean` という答えではなく、答えが無いこと**です。

もう一度訊かせるのは `r` だけです。それ以外の場合、答えはピッカーが開いている間ずっと保持されます。`Tab` で Branches ビューへ行って戻ってきても同じです。

一覧の下の詳細行には、カーソル位置の行の完全なパスが出ます。省略されたパスはここで読めます。

リポジトリごとに空行が入り、一覧が pane より長い場合は右端にスクロールバーが出ます。

エージェントの状態: `●` 実行中、`○` 待機中、`◆` ブロック中、`·` エージェントなし。herdr が `status_indicators = "symbols"` の場合は `◐`、`○`、`×`、`·` になります。

### 絞り込み

絞り込みは fuzzy で、ツリーの下方向にカスケードします。リポジトリにマッチすればその中身がすべて出ます。worktree にマッチすればその上で動いている pane も出ます。pane 単独でマッチした場合は、それがどこにあるか分かるよう上位の見出しも一緒に出ます。

自分自身はマッチせず、文脈として残っている行（結果の上のリポジトリ、マッチしたブランチ配下の pane）は一覧から消さず、dim 表示で残します。結果が「どこにあるか」を説明する構造なしに出ることはありません。

fuzzy マッチは緩く、`harken` は意外なほど多くの文字列の部分列になります。そのため結果はツリー順ではなくマッチの良さ順に並べます。意図したリポジトリが先頭に来ます。（herdr の navigator はセッション順を保ちます。あちらの行順は利用者が既に知っている順序ですが、こちらはそうではないためです。）

## Branches

3 段階です。どのリポジトリか、どのブランチか、その pane をどこに置くか。

### リポジトリを選ぶ

herdr が開いているリポジトリがすべて並びます。Panes ビューがグループ化しているのと同じ集合です。ピッカーを開いた元のリポジトリには ◆ が付き、最初からカーソルが当たっているので、今までどおりの作業は `Enter` 1 回増えるだけです。他のリポジトリへは ↓ 1 回で届き、Panes ビューを経由する必要はありません。

```
 / search repositories                                        4 repositories
 ◆ ShoMasegi/herdr-worktree-nav     1 worktree, 2 panes    ~/Workspace/herdr-worktree-nav
   ShoMasegi/harbour-backend  3 worktrees, 5 panes   ~/Workspace/harbour/harbour-backend
   nightowl/harken            1 worktree, 1 pane     ~/Workspace/nightowl/harken
   nightowl/harken_android    1 worktree, 3 panes    ~/Workspace/nightowl/harken_android
```

数はそのリポジトリがどれだけ開いているかを表します。パスは同じ fork の 2 つの checkout を見分けるためのもので、名前と一緒に検索対象になります。

| キー | 動作 |
| --- | --- |
| `↑` `↓`, `k` `j`, `Ctrl-P` `Ctrl-N` | 移動 |
| `Enter` | このリポジトリのブランチを一覧する |
| `/` | 検索 |
| `Tab` | Panes に戻る |
| `q`, `Esc`, `Ctrl-C` | 閉じる |

開いているリポジトリが 1 つだけのときは、この段階を飛ばします。選択肢が 1 つしかない画面は何も訊いていないからです。そのときの `Esc` はブランチ一覧を閉じます。

### ブランチを選ぶ

一覧の先頭には、そのブランチ群がどのリポジトリのもので、ディスク上のどこにあるかが出ます。リポジトリ一覧でカーソルの下に出ていた行が、そのまま上に移ったものです。リポジトリが 1 つのときはリポジトリ選択の段階自体が無いので、この見出しがリポジトリを名乗る唯一の場所になります。下は繰り返しません。どの行も同じリポジトリのものなので、一覧の下の breadcrumb はブランチ名から始まります。

| キー | 動作 |
| --- | --- |
| `↑` `↓`, `k` `j`, `Ctrl-P` `Ctrl-N` | 移動 |
| `Enter` | このブランチを選ぶ |
| `/` | 検索 |
| `i` | 次の並び順へ |
| `Shift-I` | 並びを反転する |
| `n` | このブランチから新しいブランチを始める |
| `f` | このリポジトリを fetch する |
| `Tab` | Panes に戻る |
| `Esc` | リポジトリ一覧に戻る |
| `q`, `Ctrl-C` | 閉じる |

### 別のブランチからブランチを始める

ブランチの上で `n` を押すと、そこから分けた新しいブランチの名前を聞かれ、続けて pane の行き先を選びます。行き先の段階は他のブランチと同じものです。

```
 + new branch from main: feat/login-v2█                           5 branches
 me/app · ~/src/app ─────────────────────────────────────────────────────────
   ● feat/login    running      #123 Add the login screen (draft)
   · main          local        latest work on main
```

プロンプトが何から分けるのかを名乗ります。`HEAD` から始まる `+ create` の申し出との違いはまさにそこだからです。基点は `n` を押した時点で確定します。その行が見えるようにリストは残りますが、カーソルはもう動きません。打っている間に基点が変わるようでは、選んだことになりません。

`Ctrl-U` で名前を消し、`Esc` で一覧に戻ります。行き先から `Esc` を押すと、打った名前が残ったままここへ戻ります。

空の名前、git が受け付けない名前、このリポジトリに既にある名前は、行き先を選んで作業が始まったあとではなく、プロンプトの時点で断ります。

リモートにしか無いブランチから始めた場合は、先に fetch してから `origin/<branch>` を基点にします。その行をそのまま選んだときと同じ挙動です。

### どちらの一覧でも検索する

どちらも Panes ビューと同じ作りです。`/` を押して検索フィールドにフォーカスが移るまで、英字はコマンドです。左端の `/` は、一覧がキーを持っている間は dim、検索フィールドが持っている間は accent 色になり、ブロックカーソルが出ます。`search …` の案内もそれに合わせて消えます。入っていないフィールドについての案内だからです。

| キー | 検索中の動作 |
| --- | --- |
| 英数字 | 絞り込み |
| `↑` `↓`, `Ctrl-P` `Ctrl-N` | 移動 |
| `Enter` | カーソル位置のものを選ぶ |
| `Backspace` | 1 文字削除 |
| `Ctrl-U` | 入力を空にする |
| `Ctrl-O` `Ctrl-R` | 並び順 / 反転 |
| `Ctrl-F` | fetch |
| `Esc` | 検索を破棄する |

`Enter` は絞り込みの確定ではなく選択です。ここが Panes と違う点です。絞り込んだブランチ一覧に対してやることは「残った 1 つを開く」ことだけで、それを言うためにもう一度 `Enter` を求めるのは儀式でしかないからです。

`Esc` は打った内容を破棄します（Panes と同じ）。それで失われるものはありません。コマンドの `Ctrl-` 形——並び順の `Ctrl-O`・`Ctrl-R`、fetch の `Ctrl-F`——は入力中もそのまま効くためです。絞り込んだ一覧を並べ替えても、絞り込みは消えません。

各リポジトリは 1 度だけ読みます。戻って別のリポジトリを選び、また戻ってきても git を再実行しません。

`Ctrl-F` はそのリポジトリに対して `git fetch origin --prune` を実行し、読み直します。一度も fetch していないブランチに日付とコミット件名が入るのはこれです（それまで `git ls-remote` は名前しか知りません）。リモートで削除済みのブランチが一覧から消えるのもこれです。消えるのは remote-tracking ref だけで、それはリモートのキャッシュです。ローカルブランチにも作業ツリーにも触れません。

実行中はプロンプトの脇に accent 色で `⠙ fetching origin…` が出ます。その間も一覧はそのまま使えます。リモートに到達できなかった場合はその行に理由が出るだけで、何も変わりません。

リモートは背景で読み込みます。ローカルの結果は即座に表示され、`git ls-remote` が返るまでプロンプト脇に `⠙ reading the remote…` が出ます。未 fetch のブランチはそれが返った時点で追加されます。オフラインの場合はこの行が消えるだけで、ローカルの一覧はそのまま使えます。

どちらの待ちにも同じスピナーが付きます。再描画ではなく時計で回るので、入力中に速くなったりキーを押しっぱなしにすると止まったりしません。動いているピッカーが固まって見えることが無いようにするためです。

### 並び順

`i` で 3 つの並び順を巡回し、`Shift-I` で現在の並びを反転します。検索フィールドに入力中は `Ctrl-O` / `Ctrl-R` が同じ働きをします（端末では `Ctrl-I` が Tab と同じで、このビューは Tab を Panes への切り替えに使っているためです）。どれが効いているかは件数の隣に出ます。既定でなくなった時点で accent 色になります。いつもと違う並びの一覧は、そう名乗るべきだからです。

| 並び | 読み方 |
| --- | --- |
| `state ↓` | 稼働中 → checkout 済み → ローカル → リモートのみ。同じ状態の中では新しい順。既定 |
| `updated ↓` | 最後にコミットした順 |
| `name ↑` | a から z |

矢印は行ではなく値を指します。↓ が降順なので、新しい順・活発な順・z が先という意味です。並びを変えると矢印はその並び自身の向きに戻ります。「日付順」と言ったときに「古い順」を意図する人はいないからです。意図したときに押すのが `Shift-I` です。

並びを変えるとカーソルは先頭の行に移ります。新しい並びにする目的は「いま何が一番上か」を見ることであり、選んでいたブランチを追いかけると、その行がたまたま落ち着いた場所——答えが無い唯一の場所——にカーソルが残るためです。反転も同じ理由で先頭に戻ります。反対側の端を見たくて押すものだからです。

どの並びでも動かないものが 2 つあります。日付を持たないブランチ（`ls-remote` でしか見えておらず、一度も fetch していないもの）は、どちらの向きでも末尾に沈みます。並びを反転した途端に、最も情報の少ない行が画面の先頭を埋めるべきではないからです。もう 1 つ、ブランチを作る提案は常に最後です。それは提案であって、そのリポジトリのブランチではありません。

並び順は入力中も維持されます。fuzzy 検索は「一覧に含めるか」を決め、並び順は「どこに置くか」を決めます。絞り込んだ結果をマッチスコア順に並べてしまうと、何か打った瞬間に、選んだばかりの並び順が黙って上書きされてしまいます。

並び順はピッカーを開いている間だけ、リポジトリをまたいで保持され、閉じると忘れられます。このプラグインはディスクに何も書きません。

### 各状態の意味

| 表示 | ブランチの状態 | `Enter` の動作 |
| --- | --- | --- |
| `● running` | いま pane で開いている | その pane へ移動 |
| `○ checked out` | worktree はあるが何も動いていない | 指定した場所にその checkout を開く |
| `· local` | ローカルブランチ、worktree なし | そこから worktree を作る |
| `↓ remote` | リモートにあるが未 fetch | fetch してから `origin/<branch>` を基点に作る |
| `+ create` | まだ存在しない（入力した名前） | `HEAD` から作成し、そこから worktree を作る |

これに加えて `gone` が付くことがあります。独立した状態ではなく、状態への注記です（`checked out gone`、`local gone`）。そのブランチが追跡している ref を git が見つけられない、という意味です。たいていはマージされた pull request の head を GitHub が削除し、prune 付きの fetch がそれに気づいた状態です。ブランチも checkout もまだここにありますが、追跡していたものはもうありません。

**一度も push していないブランチは `gone` になりません。** 追跡するものが無いことと、追跡していたものが消えたことは別であり、ピッカーはこの 2 つを混同しません。

`running` は行き先の選択を飛ばします。その作業はすでに開いているので、「二重に開いた分をどこに置くか」を尋ねるのは筋が違うためです。

`remote` は `refs/remotes/origin/<branch>` に fetch し、その ref を基点に worktree を作ります。`HEAD` を基点にすると、GitHub 上のブランチと名前だけ同じ空のブランチができてしまいます。

作成候補は常に末尾に出ます。入力が有効なブランチ名で、かつ同名のブランチが存在しなければ、他に fuzzy マッチがあっても出ます。`feat/login` が存在するときに `feat/login-v2` を作れなくなってはいけないためです。

### 行き先を選ぶ

| キー | 動作 |
| --- | --- |
| `↑` `↓`, `k` `j` | 移動 |
| `Enter` | そこに pane を開く |
| `Esc`, `Backspace` | ブランチ一覧に戻る |

```
here            split right
                split down
existing tab    w1  app / logs
                w5  harken / android
existing space  w1  app → new tab
new space       on its own
```

- **here** はピッカーを呼び出した pane を split します。
- **existing tab** はその tab を、herdr が適当と判断した pane で split します。呼び出し元の tab は一覧に出ません（「here」がそれに当たるためです）。
- **existing space** はその space に tab を追加します。
- **new space** は herdr が作った workspace のまま残します。herdr 本体の `new worktree` と同じ挙動です。

一覧の右には、カーソルがある行を選んだ場合にその tab がどうなるかが出ます。tab の実際のレイアウトに、新しい pane が実際に入る位置を描き込んだものです。概略図ではなく正確な予測です — pane を指定しない行き先は「その tab がフォーカスしている pane」を分割し、分割比は等分になります。これは herdr の実挙動と同じです。

**zoomed** な tab では図の代わりに警告が出ます。herdr は zoomed な tab への pane 移動要求に成功を返しながら移動しないため、動いたように見せるのではなく手前で止めます。zoom を解除すれば選べるようになります。

`split right` が最初から選択されているので、`Enter` `Enter` で作業中の隣にブランチが並びます。

### 作成中の表示

ブランチを開く操作は、ネットワーク越しの fetch と作業ツリー全体の checkout を伴うことがあります。ピッカーはその場に留まり、今どの段階かを表示します。

```
 ⠸ fetching origin/feat/login…
 ─────────────────────────────────────────────────────────────────────
 here            split right       w1  app / agents
                 split down        ┌──────────┬──────────┐
 existing tab    w1  app / logs    │ ● claude │ + feat/l…│
 …
 ctrl+c stop
```

一覧とプレビューはそのまま残ります。ハイライトされている行が実際に処理中の行き先で、隣の図はこれから作られる tab です。段階は `fetching origin/<branch>`、`creating the worktree for <branch>` または `opening the checkout for <branch>`、そして pane を指定の場所へ移動、の順です。

`Ctrl-C` で中断できますが、キーヒントが `ctrl+c stop` と出ている間だけです。つまり fetch までです。fetch は `refs/remotes` にしか書かないので、途中でやめても何も残りません。herdr に worktree を頼んだ後は、抜けると herdr が作った workspace が宙に浮くため、中断するキーはありません。ヒントも `working…` に変わります。

失敗した場合、ピッカーは画面を保持し、git または herdr の言い分を、失敗した段階と一緒に表示します。

```
 × fetching origin/feat/login: `git fetch …` failed: fatal: Could not read from remote…
 …
 ↵ close  esc close
```

中途半端な状態は残りません。止まったのは失敗した段階であり、それ以前の段階は何にも触れていない（fetch）か、完了しているかのどちらかです。同じメッセージは `herdr plugin log list` にも全文が残ります。

## 診断

```sh
herdr-worktree-nav dump
```

ピッカーが描画するはずのツリーをプレーンテキストで出力します。「herdr や git が妙な値を返している」のか「描画が間違っている」のかを切り分けるのに使えます。`HERDR_SOCKET_PATH` が必要なので、herdr セッション内の pane から実行してください。
